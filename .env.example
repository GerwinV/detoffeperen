# Database Configuration
# Local: postgres://postgres:password@localhost:5432/toffeperen
# Heroku: automatically set by Heroku Postgres addon
DATABASE_URL=postgres://postgres:password@localhost:5432/toffeperen

# Email Configuration (for magic link auth)
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASS=your-smtp-password
SMTP_FROM=noreply@kwekerijtoffeperen.nl

# Initial Admin Email
ADMIN_EMAIL=admin@example.com

# Site Configuration
SITE_URL=http://localhost:3000
